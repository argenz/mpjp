package s054ex;

public class Vehicle {
    public void steer(boolean right) {
        System.out.println("Steering " + (right ? "right" : "left")); //? punto di domanda
    }									// se right è true = "right", altrimenti "left";
}
